


# Key Toggle Input Processor

config ZMK_INPUT_PROCESSOR_KEYTOGGLE
    bool "Key Toggle Input Processor"
    depends on ZMK_INPUT_PROCESSOR
    help
        Enable the key toggle input processor, which can be used to toggle
        a key's state based on input events.


